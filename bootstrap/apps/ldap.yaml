apiVersion: argoproj.io/v1alpha1
kind: Application
metadata: { name: ldap, namespace: argocd }
spec:
  project: default
  source: { repoURL: git@github.com:Younesic/platform-gitops.git, targetRevision: main, path: bootstrap/manifests/ldap, directory: { recurse: true } }
  destination: { server: https://kubernetes.default.svc, namespace: ldap }
  syncPolicy:
    automated: { selfHeal: true, prune: false }
    syncOptions: [ CreateNamespace=true, ServerSideApply=true ]
